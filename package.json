{
  "name": "jszr-support-cn",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check public/app.js && node --check public/app-en.js && node --check public/category-page.js && node --check public/data/categories.js && node --check public/data/demo-content.js && node scripts/validate-categories.mjs && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
